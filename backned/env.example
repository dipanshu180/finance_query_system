# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Database Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/valuefy
MYSQL_URI=mysql://username:password@host:port/database_name

# Optional MongoDB Configuration (if different from URI)
MONGODB_DATABASE=valuefy
MONGODB_COLLECTION=clients

# Frontend Configuration (for CORS)
VERCEL_DOMAIN=your-app-name.vercel.app

# Server Configuration
PORT=8000
HOST=0.0.0.0
